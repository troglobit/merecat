EXTRA_DIST       = merecat.conf start.sh stop.sh
EXTRA_DIST      += gzip.sh redirect.sh
CLEANFILES       = *~ *.trs *.log
TEST_EXTENSIONS  = .sh

TESTS            = start.sh
TESTS           += gzip.sh
TESTS           += stop.sh

